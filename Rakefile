require 'rspec/core/rake_task'

namespace :build do
  task :all do
    puts 'build:all'
  end

  task :aws do
  end

  task :gcp do
  end

  task :vsphere do
  end
  task :azure do
  end

  task :openstack do
  end
end

RSpec::Core::RakeTask.new(:spec)

task :default => :spec
